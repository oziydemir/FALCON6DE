public class NewTry {
    public Boolean leapYear(Date dt){
        integer yearIs=dt.year();
        Boolean checkIs=date.isLeapYear(yearIs);
        return checkIs;
    }

    public Boolean leap_year_again(Date ed){
        integer yearN=ed.year();
        Boolean checkL=Date.isLeapYear(yearN);
        return checkL;
    }
}
